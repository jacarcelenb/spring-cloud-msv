spring:
  application:
    name: gateway
  profiles:
    default: eureka-on-cb
server:
    port: 4040
eureka:
  instance:
    prefer-ip-address: true
client:
  fetch-registry: true
  service-url:
    defaultZone: http://localhost:8761/eureka/
